import { Box } from '@chakra-ui/react';
import { memo } from 'react';

const Header: React.FC = () => {
  return (
    <Box bgColor="lightblue" h="60px" w="full">
      a
    </Box>
  );
};

export default memo(Header);
